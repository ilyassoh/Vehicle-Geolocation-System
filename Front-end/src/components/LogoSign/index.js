import {
  Box,
  Tooltip,
  Badge,
  tooltipClasses,
  styled,
  useTheme
} from '@mui/material';
import Link from 'src/components/Link';

function Logo() {
  const theme = useTheme();

  return (
    <>
      <h2>GEOLOCALISATION</h2>
    </>
  );
}

export default Logo;
